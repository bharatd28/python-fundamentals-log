# import os
# if(not os.path.exists("data")): # checks if dir data exists or not
#     os.mkdir("data") # if not then creates mkdir with name data
# for i in range(0,100): 
#     os.mkdir(f"data/day{i+1}") # creates more mkdir by automating naming

# if the mkdir need to renames a 100 of them 
# for i in range(100):
#     os.rename(f"data/day{i+1}",f"tutorial/day{i+1}") # this changes data/day to tutorial/day

# for more functions visit os documentation